"use strict"
module.exports = class list {
    constructor(main, title, options, calls) {
        this.main = main;
        this.title = title;
        this.options = options
        this.calls = calls
    }

    onKey(key) {
        switch (key) {
        case "UP":

            break;
        case "DOWN":

            break;
        case "ENTER":

            break;
        }
    }
}